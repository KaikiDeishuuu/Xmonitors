services:
  xmonitors:
    build: .
    container_name: xmonitors
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config.json:/app/config.json:ro
      - ./data:/app/data
